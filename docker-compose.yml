version: "3.8"

services:
  octavio-bot:
    build: .
    container_name: octavio_ai_whatsapp
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    working_dir: /app
    command: npm start
